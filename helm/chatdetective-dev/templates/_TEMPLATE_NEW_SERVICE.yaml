# ШАБЛОН ДЛЯ НОВОГО СЕРВИСА

{{- $svc := (index .Values "service-name") | default dict }}
{{- if (get $svc "enabled" | default false) }}
{{- $values := $svc }}
{{- $context := dict "Values" $values "Chart" (dict "Name" "service-name") "Release" .Release }}

---
{{ include "kind-microservice-base.deployment" $context }}

---
{{ include "kind-microservice-base.service" $context }}

---
{{ include "kind-microservice-base.ingress" $context }}

{{- end }}
