{{- $svc := (index .Values "command-handler") | default dict }}
{{- if (get $svc "enabled" | default false) }}
{{- $values := $svc }}
{{- $context := dict "Values" $values "Chart" (dict "Name" "command-handler") "Release" .Release }}

---
{{ include "kind-microservice-base.deployment" $context }}

---
{{ include "kind-microservice-base.service" $context }}

{{- end }}

