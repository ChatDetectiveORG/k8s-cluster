# Default values for local development on kind.
# Personal overrides should go to values-local.yaml (not committed).

runtime:
  # Used by all services via the runtime env ConfigMap.
  numRoutingGoroutines: 4

telegram:
  # Shared by services that call Telegram Bot API.
  # For local dev, override in values-local.yaml.
  botToken: ""
  # Used by api-gateway webhook mode (public https URL).
  publicUrl: ""
  webhookPort: "6002"

postgresql:
  # Disable Bitnami subchart for local dev (their tags may disappear / be arch-incompatible).
  enabled: false
  auth:
    username: chatdetective
    password: "chatdetective"
    database: chatdetective

localPostgresql:
  enabled: true
  image:
    repository: postgres
    tag: "16-alpine"
    pullPolicy: IfNotPresent
  resources: {}

rabbitmq:
  enabled: true
  image:
    # Bitnami removed some debian-tagged images from bitnami/rabbitmq; use legacy repo.
    registry: docker.io
    repository: bitnamilegacy/rabbitmq
    tag: 4.1.3-debian-12-r0
  auth:
    username: chatdetective
    password: "chatdetective"
  persistence:
    enabled: false

redis:
  enabled: true
  image:
    repository: redis
    tag: "7-alpine"
    pullPolicy: IfNotPresent
  auth:
    enabled: false
    # If auth.enabled=true, set a non-empty password and update REDIS_PASSWORD consumers.
    password: ""
  db: 0
  resources: {}

# --- Microservices ---

api-gateway:
  enabled: true
  replicaCount: 1
  image:
    registry: localhost:5001
    repository: chatdetective/api-gateway
    tag: latest
    pullPolicy: Always
  service:
    type: ClusterIP
    port: 6002
    targetPort: 6002
  ingress:
    enabled: false
    className: nginx
    host: ""
    pathType: Prefix
    path: /
  runtimeEnv:
    enabled: true
  env: []

command-handler:
  enabled: true
  replicaCount: 1
  image:
    registry: localhost:5001
    repository: chatdetective/command-handler
    tag: latest
    pullPolicy: Always
  service:
    type: ClusterIP
    port: 8080
    targetPort: 8080
  runtimeEnv:
    enabled: true

message-sender:
  enabled: true
  replicaCount: 1
  image:
    registry: localhost:5001
    repository: chatdetective/message-sender
    tag: latest
    pullPolicy: Always
  service:
    type: ClusterIP
    port: 8080
    targetPort: 8080
  runtimeEnv:
    enabled: true
